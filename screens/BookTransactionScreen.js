import * as React from 'react'
import {Text, View, StyleSheet, Button, TouchableOpacity} from 'react-native';

export default BookTransactionScreen extends React.Component {
    render() {
        return (
            <Text>Issue or Return</Text>
        )
    }
}